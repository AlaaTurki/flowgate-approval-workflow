spring.application.name=flowgate-backend

# Datasource (local defaults)
spring.datasource.url=jdbc:postgresql://localhost:5432/flowgate
spring.datasource.username=flowgate
spring.datasource.password=flowgatepass

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# Springdoc OpenAPI paths
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

